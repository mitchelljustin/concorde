class Range
    def self::to(finish) = self(0, finish)
    def self::from(start) = self(start, nil)

    def init(start, finish)
        self.start = start
        self.i = start
        self.finish = finish
    end

    def iter() = self

    def next()
        if self.finish != nil and self.i >= self.finish then return nil
        i = self.i
        self.i += 1
        i
    end
end

for i in Range::to(10) do io::println(i)

